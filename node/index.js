const math= require('./math.js'); 
// la instruccion require permite realizar una importación del archivo
// la instruccion ./archivo.js indica que el archivo
// se encuentra en la misma carpeta para este caso la carpeta es "node"

console.log(math.divide(1, 5));